{
  "name": "systemdac",
  "version": "1.0.0",
  "scripts": {
    "install": "npm run install:hup && npm run install:reporter",
    "install:hup": "cd ./Hup && npm install",
    "install:reporter": "cd ./Reporter && npm install",
    "add": "yarn add && yarn run add:hup && yarn run add:reporter",
    "add:hup": "yarn add --prefix ./Hup",
    "add:reporter": "yarn add --prefix ./Reporter",
    "start": "concurrently \"cross-env PORT=5173 npm start --prefix ./Hup\" \"cross-env PORT=5175 npm start --prefix ./Reporter\"",
    "build": " npm run build:hup && npm run build:reporter",
    "build:hup": "npm run build --prefix ./Hup",
    "build:reporter": "npm run build --prefix ./Reporter"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "patch-package": "^8.0.0",
    "postinstall-postinstall": "^2.1.0"
  }
}
