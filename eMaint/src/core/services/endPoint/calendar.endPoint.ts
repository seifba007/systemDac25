import { BASE_URL } from '../endpoints';

const BASE_EVENT = `${BASE_URL}/calendar`;

const LIST_EVENT = `${BASE_EVENT}/`;
const CREATE_EVENT = `${BASE_EVENT}/`;

export const CALENDAR_EVENT = {
	LIST_EVENT,
	BASE_EVENT,
	CREATE_EVENT,
};
